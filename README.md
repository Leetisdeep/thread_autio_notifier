# Disrcord selfbot Thread_Create Audio Notifier

Plays an audio from sounds folder when thread is crated at targeted channel

## Quick Setup

After installing/cloning the repository:

1. **Open terminal/bash** in the project directory
2. **Copy environment file**: `cp .env.example .env`
3. **Edit `.env` file** with your configuration values
4. **Install dependencies**: `npm install`
5. **Start the application**: `npm start`

## WARNING

its a selfbot which is not allowed by discord, do not use it 

## Environment Configuration

Make sure to configure your `.env` file with appropriate values

## Need Help?

- Ensure `.env` file exists after copying
- Run `npm install` if you encounter module errors
- Check that no other service is using the default port

---

*That's it! Your application should now be running.*

You can cange audio of sounds/notification.mp3 ofc 